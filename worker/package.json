{
  "name": "daylog-notify-worker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute daylog-db --file=./schema.sql"
  },
  "devDependencies": {
    "wrangler": "^4.0.0",
    "@cloudflare/workers-types": "^4.20250320.0",
    "typescript": "^5.9.0"
  },
  "dependencies": {
    "web-push": "^3.6.7"
  }
}
